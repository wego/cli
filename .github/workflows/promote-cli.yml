name: Promote (cli)

# THE SECOND HUMAN ACTION IN A CLI RELEASE (#74 rung 7).
#
# `release-cli.yml` published `vX.Y.Z` and advanced `cli/next`. People have been
# running that build against the real production API. This workflow is somebody
# saying it is good: it moves `cli/stable` — what everyone receives — onto the SAME
# bytes, and moves `skill/stable` behind it.
#
# PROMOTION IS A POINTER MOVE, NEVER A REBUILD. `wego update` compares CHECKSUMS,
# not versions (`src/update.ts`), so what a ring serves is what its install
# base receives on the next `update`. Rebuilding to promote would mean the bytes that
# were exercised are not the bytes that ship — the single defect this whole rework
# exists to remove. `upload-release-blob.ts --promote` server-side copies the frozen
# `cli/<tag>` blobs, which is why the barrier below can assert that `next` and
# `stable` end up byte-identical rather than merely equivalent.
#
# A PUBLISHED CLI BINARY CANNOT BE RECALLED (issue constraint 1). Nothing here
# reaches a machine that already installed. So every check this lane has is spent
# BEFORE the pointer moves: the tag must exist, `cli/next` must currently serve it,
# and the two rings' manifests must match afterwards. There is no undo step, because
# there is no undo — the recovery is promoting an earlier good tag over the top.
#
# ROLLBACK is this same workflow with an older `tag`. It is a promote like any other:
# the older tag's frozen bytes are copied onto `cli/stable`, and every install
# self-updates onto them because the checksums differ. Note `VERSION` on the ring
# then advertises the older version, which is correct — that IS what stable serves.
on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'The vX.Y.Z tag to promote to cli/stable. Must already be published and, unless you are rolling back, should be what cli/next serves.'
        required: true
        type: string
      allow_not_next:
        description: 'ROLLBACK / RECOVERY. Promote a tag that cli/next does not currently serve. Normally false: the promote decision is about the build people have been running.'
        required: false
        default: false
        type: boolean

# contents:read — this workflow creates no tag and no Release; both already exist
# (release-please cut them). It only copies blobs.
#
# actions:read is added on the promote job: one gate below asks the Actions API
# whether the release run for this tag actually succeeded.
permissions:
  contents: read

jobs:
  # THE HUMAN GATE, and nothing else.
  #
  # It is NOT an environment. On a private repository a GitHub Team plan cannot
  # carry required-reviewer protection rules - that rule is Enterprise-only, and
  # `PUT .../environments/stable-approval` answers 422 "Please ensure the billing
  # plan supports the required reviewers protection rule". Declaring the
  # environment anyway would be worse than leaving it out: GitHub creates it
  # unprotected on first use, so the job would advertise an approval gate and
  # wave every dispatch straight through.
  #
  # What gates a promote instead, both enforced:
  #   1. This workflow has NO trigger but `workflow_dispatch`, so a promote only
  #      ever happens because a person with write access typed a tag.
  #   2. This job refuses to continue unless that person is a named promoter.
  # The allow-list is meaningful because this file is itself protected: `main`
  # requires a code-owner review, so widening the list is a reviewed change.
  #
  # WHAT THIS GIVES UP, and the `stable-approval` environment did not: four eyes.
  # With environment reviewers the dispatcher and the approver were necessarily
  # different people ("prevent self-review"); here one person does both. Restoring
  # that needs GitHub Enterprise. See wego/foundations#128.
  #
  # Separate from `promote` on purpose: this job holds NO secrets and NO variables
  # - there is nothing in it to leak or misuse - and `promote` cannot begin until
  # it has passed.
  approve:
    name: Approve promoting ${{ inputs.tag }} to cli/stable
    runs-on: github-runner-small
    timeout-minutes: 5
    steps:
      - name: Only a named promoter may promote
        env:
          ACTOR: ${{ github.actor }}
          PROMOTERS: sunny-wego yeouchien-wego chuyeowego
        run: |
          set -euo pipefail
          for promoter in $PROMOTERS; do
            if [ "$promoter" = "$ACTOR" ]; then
              echo "$ACTOR is a named promoter."
              exit 0
            fi
          done
          echo "::error::$ACTOR is not a named promoter. Allowed: $PROMOTERS"
          exit 1

      - name: Record what is being approved
        env:
          TAG: ${{ inputs.tag }}
          ALLOW_NOT_NEXT: ${{ inputs.allow_not_next }}
        run: |
          set -euo pipefail
          echo "Approved: promote $TAG to cli/stable."
          if [ "$ALLOW_NOT_NEXT" = "true" ]; then
            echo "::warning::allow_not_next is set - this is the rollback path, and cli/next is NOT required to serve $TAG."
          fi

  promote:
    name: Promote ${{ inputs.tag }} to cli/stable
    needs: approve
    runs-on: github-runner-small
    timeout-minutes: 30
    # Serialized against itself AND against nothing else: `release-cli.yml` writes
    # `cli/next`, this writes `cli/stable`, so the two lanes share no pointer and must
    # not share a queue slot. cancel-in-progress is FALSE for the same reason it is
    # there: a promote aborted between copying binaries and copying the manifest
    # leaves `stable` half-moved.
    concurrency:
      group: ring-stable
      cancel-in-progress: false
    # The store token lives here. `approve` gated entry to this job; this
    # environment is what actually holds the credential that selects a store.
    environment:
      name: production
    permissions:
      contents: read
      # One gate below queries the Actions API for this tag's release run.
      actions: read
    env:
      WEGO_CLI_TELEMETRY: "0"
    steps:
      # `ref` is the PROMOTED TAG, not the default branch. The plugin publisher
      # below reads the tag's own tree, so a default checkout would publish main's
      # plugin content beside the tag's binaries. A promote is a pointer move onto
      # ONE revision's bytes, and everything published beside them is part of that
      # revision. (In wego-ai the same rule covered the skill body, which this
      # repository ships embedded in the binary instead.)
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.tag }}
          persist-credentials: false
          fetch-depth: 0

      - uses: ./.github/actions/setup-bun

      # Shape-gate the typed tag before it reaches the store. This is the ONE input a
      # human types in the whole release, so it is the one place a typo can enter:
      # `validate-release-tag.ts` is the same authority the release lane uses, and
      # `ring-rules.ts` refuses a prerelease on `stable` independently at publish
      # time. Checked here so a bad tag costs one cheap step, not a Blob round trip.
      - name: Validate the tag
        env:
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          bun run scripts/validate-release-tag.ts "$TAG"
          case "${TAG#v}" in
            *-*)
              echo "::error::$TAG is a prerelease. cli/stable serves only a plain X.Y.Z - a prerelease belongs on cli/edge, which edge-cli.yml publishes."
              exit 1 ;;
          esac
          echo "ok: $TAG is a plain release version."

      # A tag can exist - and `cli/<tag>/` can even be half-written - without the
      # release lane ever having FINISHED. Promoting that would put bytes no release
      # run ever verified in front of every install, which is the whole install base.
      #
      # UNCONDITIONAL, including under `allow_not_next`: that input relaxes WHICH
      # ring must currently serve the tag (the rollback path), never whether the tag
      # was released at all. A rollback target is a tag that was released and then
      # superseded, so this gate costs a rollback nothing.
      #
      # `branch=<tag>` is how the Actions API filters runs by ref name; a tag-push
      # run is recorded under the tag's own name.
      # `actions/github-script`, NOT `gh api`. This runs on `github-runner-small`,
      # a self-hosted image with no GitHub CLI on PATH: `gh api` here exited 127
      # `gh: command not found` and took the whole promote down with it
      # (wego/foundations#129, run 34609251560). GitHub-HOSTED runners ship `gh`,
      # which is why the shape looks fine until you run it. `release-cli.yml`
      # already reaches the API this way and uses no `gh` anywhere; this was the
      # only `gh` invocation in either workflow. The job's `actions: read` is
      # what authorises the listing.
      - name: Require a completed, successful release run for the tag
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const tag = process.env.TAG;
            const { data } = await github.rest.actions.listWorkflowRuns({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "release-cli.yml",
              branch: tag,
              per_page: 100,
            });
            const ok = data.workflow_runs.filter(
              (r) => r.status === "completed" && r.conclusion === "success",
            ).length;
            if (ok < 1) {
              core.setFailed(
                `No completed, successful release-cli.yml run for ${tag}. cli/stable serves every install, so it is only ever advanced onto bytes a release run finished and verified. Re-run or fix the release for ${tag} first; allow_not_next does not bypass this.`,
              );
              return;
            }
            core.info(`ok: release-cli.yml completed successfully for ${tag} (${ok} run(s)).`);
        env:
          TAG: ${{ inputs.tag }}

      # foundations#101 rung 5(f). ASKED BEFORE THE RINGS MOVE, ON PURPOSE.
      #
      # The two plugin steps below exit 0 when the tag's tree carries no publisher.
      # That guard is right - a promote must not hard-fail on a missing file after
      # cli/stable has moved - but with REQUIRE_PUBLISH on it produces the one shape
      # this workflow refuses everywhere else: a promote that published nothing and
      # reported success. REQUIRE_PUBLISH cannot catch it, because the variable is
      # read inside `publish-plugin.ts` and the guard fires before that script runs.
      #
      # So the question moves here, where "no" is free: nothing has moved yet.
      #
      # A ROLLBACK IS THE ONE CASE WHERE SKIPPING IS CORRECT. Rolling back to a
      # pre-lane tag is the documented recovery path, and such a tag has no plugin
      # content to publish - there is nothing to roll the plugin repo back TO. That
      # path stays open and warns instead, because a rollback that cannot run is a
      # worse failure than a plugin repo that lags by one tag.
      - name: Refuse a tag whose tree cannot publish the plugin
        if: vars.SKILLS_PUBLISH_ENABLED == 'true'
        env:
          TAG: ${{ inputs.tag }}
          ALLOW_NOT_NEXT: ${{ inputs.allow_not_next }}
        run: |
          set -euo pipefail
          missing=''
          for f in scripts/publish-plugin.ts scripts/verify-plugin-published.ts; do
            [ -f "$f" ] || missing="$missing $f"
          done
          if [ -z "$missing" ]; then
            echo "ok: $TAG carries the plugin publish lane."
            exit 0
          fi
          if [ "$ALLOW_NOT_NEXT" = "true" ]; then
            echo "::warning::$TAG predates the plugin publish lane (missing:$missing). This is the rollback path, so the promote continues - but wego/skills will keep serving the plugin content of a NEWER tag than cli/stable. Promote a plugin-carrying tag to resynchronise."
            exit 0
          fi
          echo "::error::$TAG has no plugin publish lane in its tree (missing:$missing) and SKILLS_PUBLISH_ENABLED is on. Refusing before cli/stable moves: promoting would move both rings and then skip the publish, which reports success for a promote that published nothing. Re-run with allow_not_next if this is a deliberate rollback to a pre-lane tag."
          exit 1

      # THE PROMOTE DECISION IS ABOUT A BUILD PEOPLE RAN, and that gate lives in the
      # publisher (`--require-serving next`), not here.
      #
      # It has to, because of WHICH STORE it asks. A step here could only reach a
      # `GET /install?dl=VERSION&ring=next`, which resolves against that deploy's own
      # `CLI_DOWNLOAD_BASE_URL` — while the promote writes to the Blob origin the
      # publisher discovers from `cli/<tag>/`. Nothing enforces that those are the
      # same store, so a workflow-side gate can pass or refuse on a fact about the
      # wrong place. The publisher already holds the origin it is about to write, so
      # it is the only place the gate and the write are the same store by
      # construction.
      #
      # `allow_not_next` drops the flag rather than adding a bypass: a rollback
      # promotes an OLDER tag, which by definition is not what `next` serves.

      # THE PROMOTE GUARD (foundations#132, #134, #159 item 4). Refuse to move
      # `stable` unless the legacy bridge pin is LIVE in front of it.
      #
      # The guarantee this protects: every 1.0.0 and 1.0.1 install following
      # `stable` reaches every later release with nothing but `wego update`, at any
      # time, with no deadline and no reinstall. Those binaries trust only wego-ai's
      # signing identity. The moment `stable` names a release signed under
      # wego/cli's, they can no longer verify what `stable` points at — they refuse,
      # fail-closed, and stay refusing. There is no later fix: a binary that cannot
      # verify an update cannot be updated INTO one that can.
      #
      # So the pin has to already be answering before this pointer moves, and the
      # only honest way to know that is to ASK api.wego.com the way an old binary
      # asks it — with its user agent, for the object it actually fetches. A 1.0.x
      # `wego update` sends `Bun/…` because the 1.0.x CLI never named itself; the
      # pin keys on that prefix and nothing else. If the answer names the bridge
      # prefix, an old binary is being served a release it can verify, and `stable`
      # is free to move past it.
      #
      # **This refuses today, and that is correct.** The bridge does not exist until
      # 3a publishes it and sets the two variables. It passes from the end of 3a on.
      # There is deliberately no bypass input: a promote that needs to skip this is
      # a promote that strands the 1.0.x install base, and `allow_not_next` does not
      # reach it either — a rollback moves `stable` just as surely, so it needs the
      # pin just as much.
      #
      # Production, not the promote's own store origin, on purpose: unlike the
      # `--require-serving next` gate below — which must ask the store it is about
      # to write — this one is about what INSTALLED BINARIES are being served, and
      # `api.wego.com` is the host every one of them resolves through.
      - name: Require the legacy bridge pin to be live
        run: |
          set -euo pipefail
          loc=$(curl -sI -A 'Bun/1.3.14' \
                  'https://api.wego.com/install?dl=SHA256SUMS.txt&ring=stable' \
                  | grep -i '^location:' || true)
          if printf '%s' "$loc" | grep -q '/cli/cli-v1.1.0/'; then
            echo "ok: an old (Bun/) binary asking stable is served from the bridge prefix."
            echo "$loc"
            exit 0
          fi
          echo "::error::the legacy bridge pin is NOT live: a Bun/ user agent asking for stable is answered with '${loc:-no location header}', which does not name /cli/cli-v1.1.0/. Moving cli/stable now would strand every 1.0.0 and 1.0.1 install that follows it, permanently and unrecoverably. Publish the bridge and set CLI_LEGACY_BRIDGE_RING + CLI_LEGACY_BRIDGE_TAG on the production apps/api deploy first (wego/foundations#132)."
          exit 1

      # THE COMPLEMENT OF THE PROBE ABOVE, and the check whose absence shipped
      # v1.2.0 (wego/cli#25).
      #
      # That probe asserts OLD binaries are pinned. This asserts the binary we
      # are about to put in front of everybody is NOT - by running its real
      # `wego update` against the real production route, in a throwaway
      # directory, BEFORE the pointer moves.
      #
      # `stable` still serves the outgoing version here, so a correct binary
      # performs a genuine update and lands on it (a downgrade, in a temp dir,
      # affecting nothing). That is the point: a successful round trip proves
      # the whole chain - user agent, pin decision, record verification,
      # manifest coverage, hash comparison, replace - rather than one property
      # that is supposed to imply it. v1.2.0's binary refuses here with exit 6.
      #
      # DELIBERATELY INLINE, not `scripts/assert-self-update.ts`: this job checks
      # out the TAG's tree, so a script-based check would silently skip on every
      # tag cut before it existed - including the one tag this must refuse.
      #
      # Skipped on a rollback: `allow_not_next` promotes a tag `next` does not
      # serve, so there is nothing at `ring=next` to fetch, and a rollback target
      # is by definition a build that already shipped.
      - name: Require this tag's binary to be able to update itself
        if: ${{ !inputs.allow_not_next }}
        env:
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          url='https://api.wego.com/install'
          work="$RUNNER_TEMP/selfupdate"; rm -rf "$work"
          # The binary MUST be named `wego`: the config directory is derived from
          # the name it is invoked as, so `wego-linux-x64` looks for
          # `.config/wego-linux-x64/`, finds no install record, and exits without
          # touching the network - a green check that tested nothing.
          mkdir -p "$work/bin" "$work/home/.config/wego"
          # `--require-serving next` in `Advance cli/stable` guarantees cli/next IS
          # this tag - but that runs AFTER this step, so a mismatch would surface
          # here first as a bare curl error. Say it plainly instead, and point at
          # the gate that owns the rule.
          served=$(curl -fsSL "$url?dl=VERSION&ring=next" || true)
          if [ "$served" != "${TAG#v}" ]; then
            echo "::error::cli/next serves '${served:-nothing}', not ${TAG#v}, so the bytes this gate would test are not $TAG's. Promote the build that ring has been running, or use allow_not_next if this is a deliberate rollback (which skips this gate). The publisher refuses this too, at Advance cli/stable."
            exit 1
          fi
          if ! curl -fsSL "$url?dl=wego-linux-x64&ring=next" -o "$work/bin/wego"; then
            echo "::error::could not download wego-linux-x64 from cli/next at $url. The ring reports ${TAG#v} but is not serving a complete artifact set."
            exit 1
          fi
          chmod +x "$work/bin/wego"
          printf '{"ring":"stable","installUrl":"%s"}\n' "$url" \
            > "$work/home/.config/wego/install.json"

          before=$(HOME="$work/home" XDG_CONFIG_HOME="$work/home/.config" \
                   WEGO_CLI_TELEMETRY=0 "$work/bin/wego" version)
          echo "candidate binary reports $before"

          set +e
          out=$(HOME="$work/home" XDG_CONFIG_HOME="$work/home/.config" \
                WEGO_CLI_TELEMETRY=0 "$work/bin/wego" update -y 2>&1)
          code=$?
          set -e
          echo "$out"

          # `update` returns 0 without acting for a dev-versioned or from-source
          # binary (update.ts:364), so an exit-code-only check could pass having
          # done nothing.
          if printf '%s' "$out" | grep -q 'Self-update applies to installed release binaries'; then
            echo "::error::$TAG's binary does not consider itself a release build, so update did nothing and this check proved NOTHING. It was built without WEGO_BUILD_VERSION."
            exit 1
          fi
          if [ "$code" -ne 0 ]; then
            echo "::error::$TAG's binary CANNOT UPDATE ITSELF (exit $code). Promoting it would put a build in front of every install that can never receive another release. Exit 6 means it fetched a signed record it does not accept - if the message names a tag it was not built to trust, it is being served the legacy bridge, which means it sends a Bun/ user agent (wego/cli#25)."
            exit 1
          fi
          after=$(HOME="$work/home" XDG_CONFIG_HOME="$work/home/.config" \
                  WEGO_CLI_TELEMETRY=0 "$work/bin/wego" version)
          echo "ok: $TAG's binary completed a real self-update against $url ($before -> $after). cli/stable has not moved."

      # THE MOVE. A server-side copy of the frozen cli/<tag> blobs onto cli/stable.
      # REQUIRE_PUBLISH: an absent token must FAIL here rather than exit 0 having
      # promoted nothing - a "successful" promote that moved no pointer would read as
      # shipped when the whole install base is still on the old build.
      - name: Advance cli/stable
        id: promote
        env:
          BLOB_READ_WRITE_TOKEN: ${{ secrets.BLOB_READ_WRITE_TOKEN }}
          REQUIRE_PUBLISH: 'true'
          TAG: ${{ inputs.tag }}
          ALLOW_NOT_NEXT: ${{ inputs.allow_not_next }}
        run: |
          set -euo pipefail
          if [ "$ALLOW_NOT_NEXT" = "true" ]; then
            echo "::warning::allow_not_next is set - promoting $TAG to cli/stable without requiring that cli/next serves it. This is the rollback path."
            bun run scripts/upload-release-blob.ts --promote "$TAG" --to stable
          else
            bun run scripts/upload-release-blob.ts --promote "$TAG" --to stable --require-serving next
          fi

      # Verify the ring now serves this version, every asset hash-checked against the
      # manifest it just committed to.
      - name: Verify cli/stable
        if: steps.promote.outputs.store_origin != ''
        env:
          BASE: ${{ steps.promote.outputs.store_origin }}/cli/stable
          VERSION: ${{ inputs.tag }}
        run: sh scripts/verify-published.sh "$BASE" "${VERSION#v}" all

      # THE BYTE-IDENTITY PROOF the issue's End state asks for: `SHA256SUMS.txt` read
      # from BOTH rings and compared at promote time.
      #
      # This is the claim the whole design rests on - that what the candidate group
      # exercised is exactly what everyone receives - and it is checkable in one line
      # precisely because a promote is a copy. If these ever differ, something
      # rebuilt, and the rebuild is the bug.
      #
      # Skipped when `allow_not_next` is set: a rollback deliberately puts `stable`
      # on a DIFFERENT build from `next`, so the manifests are expected to differ and
      # asserting otherwise would fail every rollback.
      - name: Prove cli/next and cli/stable are byte-identical
        if: ${{ steps.promote.outputs.store_origin != '' && !inputs.allow_not_next }}
        env:
          ORIGIN: ${{ steps.promote.outputs.store_origin }}
        run: |
          set -euo pipefail
          curl -fsSL "$ORIGIN/cli/next/SHA256SUMS.txt"   > "$RUNNER_TEMP/next.sums"
          curl -fsSL "$ORIGIN/cli/stable/SHA256SUMS.txt" > "$RUNNER_TEMP/stable.sums"
          # `sort` because the manifests are generated by separate `sha256sum *` runs
          # and the shell glob's order is not a promise; the SET of name→digest pairs
          # is the claim, not the line order.
          if ! diff -u <(sort "$RUNNER_TEMP/next.sums") <(sort "$RUNNER_TEMP/stable.sums"); then
            echo "::error::cli/next and cli/stable do not serve identical bytes. A promote is a server-side COPY of the frozen cli/<tag> blobs, so this can only mean something rebuilt between publish and promote. Do not paper over it - find the rebuild."
            exit 1
          fi
          echo "ok: cli/next and cli/stable serve byte-identical artifacts."
          cat "$RUNNER_TEMP/stable.sums"

      # THE "LEAVING" TEST. The pre-move gate proved this binary can update
      # against the OUTGOING stable. This proves the state we just created is
      # self-consistent: a binary of the version now on `stable`, asking
      # `stable`, must report it is already current.
      #
      # Post-move on purpose, and safe to be: it asserts a property of the ring
      # as it now stands, which cannot be known before the move. The gate that
      # must never fail late is the pre-move one above; this is verification,
      # the same role `Verify cli/stable` plays.
      - name: Verify a promoted binary reports itself current
        if: steps.promote.outputs.store_origin != ''
        env:
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          url='https://api.wego.com/install'
          work="$RUNNER_TEMP/selfupdate-after"; rm -rf "$work"
          mkdir -p "$work/bin" "$work/home/.config/wego"
          curl -fsSL "$url?dl=wego-linux-x64&ring=stable" -o "$work/bin/wego"
          chmod +x "$work/bin/wego"
          printf '{"ring":"stable","installUrl":"%s"}\n' "$url" \
            > "$work/home/.config/wego/install.json"

          set +e
          out=$(HOME="$work/home" XDG_CONFIG_HOME="$work/home/.config" \
                WEGO_CLI_TELEMETRY=0 "$work/bin/wego" update -y 2>&1)
          code=$?
          set -e
          echo "$out"

          if [ "$code" -ne 0 ]; then
            echo "::error::a binary taken from cli/stable cannot update itself (exit $code). cli/stable now serves $TAG and every install following it is affected. Roll back with wego-ai's promote-cli.yml (tag=cli-v1.1.0, allow_not_next=true) and see wego/cli#25."
            exit 1
          fi
          if ! printf '%s' "$out" | grep -qi 'up to date'; then
            echo "::error::a binary taken from cli/stable did not report itself current - it replaced itself. cli/stable is not serving the bytes this promote just copied."
            exit 1
          fi
          echo "ok: a binary from cli/stable asks stable and is told it is current."

      # NO skill steps. The skill channel did not move to this repository: the
      # SKILL.md ships embedded in the binary, and `update` re-runs the new
      # binary's `skill install --owned-only` after it swaps. In wego-ai this job
      # advanced `skill/stable` beside `cli/stable`, and `release-cli.yml` advanced
      # `skill/next` beside `cli/next`.

      - name: Mint the plugin publish token
        id: plugin-token
        if: steps.promote.outputs.store_origin != '' && vars.SKILLS_PUBLISH_ENABLED == 'true'
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          client-id: ${{ vars.SKILLS_PUBLISH_APP_CLIENT_ID }}
          private-key: ${{ secrets.SKILLS_PUBLISH_APP_PRIVATE_KEY }}
          owner: wego
          repositories: skills
          permission-contents: write

      # The `SKILLS_PUBLISH_ENABLED` guard belongs on THIS step too, not only on
      # the two above it. Without it the flag is half-honoured: with the flag off,
      # `Refuse a tag whose tree cannot publish the plugin` and `Mint the plugin
      # publish token` skip, so `steps.plugin-token.outputs.token` is empty — and
      # then this step runs anyway with `REQUIRE_PUBLISH: true`, the script reports
      # `SKILLS_PUBLISH_TOKEN is unset - skipping the skill publish`, and exits 1.
      # The promote goes red AFTER `cli/stable` has already advanced and been
      # verified, which is the worst possible place to fail: the ring moved, the
      # run says failure. Observed in wego/foundations#129, run 34610149769.
      #
      # `REQUIRE_PUBLISH: 'true'` stays. It is right when the flag is ON — a skip
      # there would be a promote that silently published nothing.
      - name: Publish the plugin repo
        if: steps.promote.outputs.store_origin != '' && vars.SKILLS_PUBLISH_ENABLED == 'true'
        env:
          SKILLS_PUBLISH_TOKEN: ${{ steps.plugin-token.outputs.token }}
          REQUIRE_PUBLISH: 'true'
        run: |
          # THE WORKFLOW IS main'S; THE TREE IS THE TAG'S. This job checks out
          # `ref: inputs.tag`, but a workflow_dispatch runs the step definitions
          # from the default branch - so a promote of any tag cut before this
          # lane landed runs THESE steps against a tree that does not contain
          # its scripts. Without this guard that is a hard failure on a missing
          # file, AFTER cli/stable and skill/stable have already moved, which is
          # the one thing this workflow's own comments say must never happen.
          if [ ! -f "scripts/publish-plugin.ts" ]; then
            echo "scripts/publish-plugin.ts is not in this tag's tree - skipping."
            exit 0
          fi
          if [ -n "${SKILLS_PUBLISH_TOKEN:-}" ]; then
            # `http.extraheader`, NOT a token in the remote URL. The publisher
            # prints the remote in three places, one of them a `git clone` line
            # an operator is told to paste; a URL-embedded token would ride into
            # all three, and a pasteable command containing `***` is a broken
            # command rather than a safe one.
            AUTH=$(printf 'x-access-token:%s' "$SKILLS_PUBLISH_TOKEN" | base64 | tr -d '\n')
            # The base64 is a DERIVED value: Actions masks the token it minted,
            # not this encoding of it, so a verbose git trace would otherwise
            # print the header in clear.
            echo "::add-mask::$AUTH"
            # THE CREDENTIAL NEVER TOUCHES DISK. `GIT_CONFIG_COUNT` injects
            # config into git's process environment, so there is no file to
            # leak, nothing to clean up, and nothing left behind when a run is
            # killed before its cleanup step - which matters because
            # `github-runner-small` is self-hosted (.github/actionlint.yaml),
            # `$HOME` outlives the job, and this repo already records that a run
            # killed by a timeout or an OOM never reaches its own cleanup
            # (post-deploy-staging-smoke.yml).
            #
            # It ADDS to the runner's real config rather than replacing it, which
            # `GIT_CONFIG_GLOBAL` does not: measured, redirecting that variable
            # hides `~/.gitconfig` entirely, `init.defaultBranch` included - and
            # which branch `git push origin HEAD` resolves to on an empty remote
            # is the one failure rung 4b's read-back exists to catch, so this
            # lane must not quietly change its inputs.
            #
            # Measured: the injected key is visible to git, the real global
            # config is still visible beside it, `~/.gitconfig` is untouched,
            # and the value survives into the `git` that `bun` spawns.
            export GIT_CONFIG_COUNT=1
            export GIT_CONFIG_KEY_0="http.https://github.com/.extraheader"
            export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic $AUTH"
            # `http.<url>.extraheader` matches by URL PREFIX, so this
            # write-scoped token would attach to ANY github.com git command that
            # ran later in this step. Nothing else here runs git - keep it that
            # way, or scope the header to the exact remote first.
            export SKILLS_PLUGIN_REPO="https://github.com/wego/skills.git"
          fi
          bun run scripts/publish-plugin.ts

      # foundations#101 rung 4b. The publish above proves the repo holds only
      # paths the plan names; it never reads back what those paths CONTAIN. This
      # does, from an independent clone - the only vantage point that can see a
      # push which landed on a branch the clone does not check out, which exits 0
      # in the step above and leaves the channel empty.
      #
      # It builds its own credential rather than inheriting one: the publish
      # step's lives only in that step's process environment, so there is
      # nothing to reuse and nothing left behind if this step is removed.
      # Same guard, same reason: with the flag off there is no token and nothing
      # was published, so there is nothing to verify.
      - name: Verify the plugin repo
        if: steps.promote.outputs.store_origin != '' && vars.SKILLS_PUBLISH_ENABLED == 'true'
        env:
          SKILLS_PUBLISH_TOKEN: ${{ steps.plugin-token.outputs.token }}
        run: |
          # THE WORKFLOW IS main'S; THE TREE IS THE TAG'S. This job checks out
          # `ref: inputs.tag`, but a workflow_dispatch runs the step definitions
          # from the default branch - so a promote of any tag cut before this
          # lane landed runs THESE steps against a tree that does not contain
          # its scripts. Without this guard that is a hard failure on a missing
          # file, AFTER cli/stable and skill/stable have already moved, which is
          # the one thing this workflow's own comments say must never happen.
          if [ ! -f "scripts/verify-plugin-published.ts" ]; then
            echo "scripts/verify-plugin-published.ts is not in this tag's tree - skipping."
            exit 0
          fi
          if [ -n "${SKILLS_PUBLISH_TOKEN:-}" ]; then
            AUTH=$(printf 'x-access-token:%s' "$SKILLS_PUBLISH_TOKEN" | base64 | tr -d '\n')
            echo "::add-mask::$AUTH"
            export GIT_CONFIG_COUNT=1
            export GIT_CONFIG_KEY_0="http.https://github.com/.extraheader"
            export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic $AUTH"
            export SKILLS_PLUGIN_REPO="https://github.com/wego/skills.git"
          fi
          bun run scripts/verify-plugin-published.ts

      - name: Summary
        if: steps.promote.outputs.store_origin != ''
        env:
          TAG: ${{ inputs.tag }}
        run: |
          {
            echo "### Promoted \`$TAG\` to \`cli/stable\`"
            echo
            echo "Everyone on \`wego update\` receives this build from now on."
            echo "It is byte-identical to what \`cli/next\` served."
          } >> "$GITHUB_STEP_SUMMARY"

  # P9 (foundations#109): the ONLY outbound channel these lanes have.
  #
  # Nothing here has ever told a person that a release, promote or rollback
  # failed. The triggering actor on the production lanes is a bot, so GitHub's
  # own "your run failed" mail reaches nobody and the red run just sits on the
  # Actions tab. The action this calls opens one issue per lane; see its header
  # for why an issue and not a chat webhook.
  #
  # `cancelled` is reported next to `failure` deliberately: a job killed by its
  # `timeout-minutes` bound (H1, wego-ai#1863) reports as `cancelled`, NOT as
  # `failure`. A lane that hung is exactly the case nobody hears about.
